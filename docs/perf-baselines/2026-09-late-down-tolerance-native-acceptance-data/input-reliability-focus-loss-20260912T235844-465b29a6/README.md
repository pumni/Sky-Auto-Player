# Exact-head focus-loss regression

This isolated `focus-loss` scenario was run on implementation head
`b8bbe42d84837c440dd2e51cc90b490298aaa760` with a clean tracked source tree.
It passed on the project-owned `ReceiveOnly` sink. The inert focus probe
received zero keyboard events. The directory retains the filtered runner,
configuration, native report, invocation ledger, ready records, and both raw
event logs.
