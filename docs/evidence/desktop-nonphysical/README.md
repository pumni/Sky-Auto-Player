# Desktop non-physical slice evidence

Captured from the real Windows Tauri window on 2026-08-29 using the debug shell
and the production `src/core_main.py` admission path. The Core process was the
real local worker; no mock bridge or physical playback was used.

- `library-real-tauri.png` — default Library view, 96 songs.
- `search-real-tauri.png` — real search for `blue`.
- `detail-real-tauri.png` — selected song detail and timing recommendation.
- `settings-real-tauri.png` — settings modal and Core-backed settings surface.
- `minimum-real-tauri.png` — Settings view at the configured minimum window size
  of 920×620.
