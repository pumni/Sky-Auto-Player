# Sky Auto Player v3.0.0 Acceptance Packet

Release: v3.0.0
Candidate SHA: `da0cf49b79782b2bec8933c2512ac1aafcc03969`
Date: 2026-08-03
Reviewer: Codex local release run; human review pending
Windows version/build: Windows 11, `10.0.26200.0`
CPU: `AMD64 Family 23 Model 104 Stepping 1, AuthenticAMD` (benchmark host fingerprint)
Architecture: AMD64 / x86_64-pc-windows-msvc
QPC frequency: `10000000 Hz`
Python: CPython `3.14.3+freethreaded`
GIL enabled: `false`
Rust: `1.97.1`
PyO3: `0.29.0`
Maturin: `1.14.1`

This packet records evidence for the code candidate SHA above. It is a
documentation-only commit and intentionally does not self-reference its own
future commit SHA.

## Version and schemas

- Application version: `3.0.0` in `pyproject.toml`, `uv.lock`, generated runtime version, and active site JSON-LD.
- Native telemetry wire schema: `6` (unchanged).
- Telemetry summary schema snapshot: `telemetry_summary_schema_v1.json`, with `sender_clean_known` and `noop_skipped_key_count` paths.
- Calibration final artifact schema: `5` (unchanged).
- Native build metadata reported by the wheel: build ID and source fingerprint both match the candidate SHA/fingerprint below.

## Static and unit evidence

| Gate | Exact command/result |
|---|---|
| Frozen dependency sync | `uv sync --frozen` — PASS; project installed as `sky-auto-player==3.0.0`. |
| Ruff | `uv run --env-file .env ruff check .` — PASS. |
| Pyright | `uv run --env-file .env pyright` — PASS, 0 errors/warnings/information. |
| Security audit | `uv run --env-file .env python scripts/audit_security_mandates.py` — PASS. |
| Free-threaded wheel audit | `uv run --env-file .env python scripts/audit_free_threaded_wheels.py` — PASS; GIL disabled and runtime wheels import. |
| Rust format | `cargo fmt --manifest-path rust/Cargo.toml --all -- --check` — PASS. |
| Rust check | `cargo check --manifest-path rust/Cargo.toml --workspace --all-targets --all-features` — PASS. |
| Rust clippy | `cargo clippy --manifest-path rust/Cargo.toml --workspace --all-targets --all-features -- -D warnings` — PASS. |
| Rust tests | `cargo test --manifest-path rust/Cargo.toml --workspace --all-features` — PASS; all reported Rust test groups passed. |
| Native wheel | `uv run --env-file .env python scripts/build_rust_wheel.py` — PASS; `cp314t-win_amd64`, PyO3 0.29.0, GIL disabled, clean candidate provenance. |
| Focused pytest | `uv run --env-file .env pytest tests/test_native_telemetry_ingest.py tests/test_native_calibration.py tests/test_runtime_dispatch.py tests/test_rust_phase7_native_engine.py tests/test_version_consistency.py tests/test_version_cli.py` — 201 passed, 2 skipped. Skips require a foreground window. |
| Fast pytest | `uv run --env-file .env pytest -m "not slow"` — 1108 passed, 10 skipped, 1 xpassed. |
| Full pytest | `uv run --env-file .env pytest` — 1108 passed, 10 skipped, 1 xpassed. |
| PowerShell 5.1 updater gate | BOM check plus `powershell.exe` parser — PASS. |
| Pester | Pester 5.9.0, `Invoke-Pester -Path .\installer\Tests -PassThru` — 61/61 passed. The first sandboxed attempt was blocked by registry policy; the rerun with the required host permission passed. |

The skipped tests are the strict focus tests at
`tests/test_rust_phase7_native_engine.py:211` and `:256`; this host had no
foreground window. The xpass is the existing Windows Textual timing test and
was not changed by this release.

## Native acceptance evidence

Exact command:

```text
uv run --env-file .env python scripts/bench_native_acceptance.py --actions 256 --repeats 3 --polyphony 1,2,3,5,8,15 --baseline .github/native_acceptance_baseline.json --label release
```

- Candidate SHA: `da0cf49b79782b2bec8933c2512ac1aafcc03969`
- Baseline SHA256: `f6f0aa57549fa7f4ab6aee69d4072fa80d8541088c2cb19f1175bf1619a8a245`
- Baseline changed: no.
- Backend: deterministic mock/sender-side simulation; no game process was touched.
- Repeats: `3`; requested actions: `256`; observed actions per polyphony: `512`.
- Polyphony matrix: `1, 2, 3, 5, 8, 15`.
- Samples: `n=9216` completion records overall; 18 polyphony/repeat runs.
- Exit code: `0`.
- Regression decision: PASS against the checked-in baseline.

Overall sender-completion summary, in microseconds:

| p50 | p95 | p99 | max |
|---:|---:|---:|---:|
| 663 | 2217 | 2572 | 4577 |

| Polyphony | p50 | p95 | p99 | max |
|---:|---:|---:|---:|---:|
| 1 | 624 | 1834 | 2304 | 2980 |
| 2 | 927 | 2322 | 2783 | 4231 |
| 3 | 630 | 2529 | 2957 | 4577 |
| 5 | 614 | 1850 | 1979 | 2730 |
| 8 | 646 | 2098 | 2355 | 2752 |
| 15 | 1144 | 2127 | 2379 | 2955 |

The run reported zero dropped keys, failed releases, chord splits, partial
SendInput events, and zero-progress failures. It is sender-side evidence only.

## Frozen application evidence

Exact command:

```text
uv run --env-file .env python -m build_app --manifest
```

Result: PASS. `build_app` completed all native calibration, PyInstaller,
manifest, optimize, Textual, and Rust mock-worker smoke gates.

Release directory: `dist/Sky-Auto-Player-v3.0.0/`
Manifest version: `3.0.0`
Manifest Git SHA: `da0cf49b79782b2bec8933c2512ac1aafcc03969`
Manifest `dirty_worktree`: `false`
Native build commit: `da0cf49b79782b2bec8933c2512ac1aafcc03969`
Native source fingerprint: `a8dc4f6ae37d00a44fe4c452589f8f8412df593631085135fb05ce1bbada1f37`
Manifest file count: `704`

| Asset | SHA256 |
|---|---|
| `Sky-Auto-Player.exe` | `4f00378e17622c6526108c426809fe0914b5d1de9162140b63d9405c53e431ed` |
| `native_calibration.exe` | `d40bbb3c616be751cb4af2da32e85485577dd492ab66b413ba4c7787e36ce39b` |
| `MANIFEST.json` | `6088a5463364f88f6c76f270e385dbe198f070ef5b835b62076bdd1c3ec994a2` |

Post-build packaged checks:

- `Sky-Auto-Player.exe --version` — PASS, reports `sky-auto-player 3.0.0`.
- `--selftest-textual` — PASS.
- `--selftest-optimize` — PASS, `__debug__` false.
- `--selftest-rust` — PASS, native module imported and mock worker terminated.
- Bounded packaged dry-run of `.init.json` — PASS; 195 notes, 1:17 simulation, no input sent.
- A 1529-note packaged dry-run loaded its schedule but exceeded the 180-second command timeout; process cleanup was confirmed. This attempt is not counted as PASS.

## Runtime lifecycle and rollback evidence

These statuses distinguish diagnostic/mock evidence from a packaged run against
a real target window. No game process or memory was inspected or modified.

| Scenario | Evidence/status |
|---|---|
| A — normal completion | PARTIAL: packaged bounded dry-run and full mock/unit suites pass; real target-window soak not run. |
| B — pause/resume | PARTIAL: pause/lifecycle tests pass; packaged target-window scenario not run. |
| C — focus loss/recovery | INCONCLUSIVE for acceptance: lifecycle tests pass, but two strict foreground-window tests were skipped because no foreground window was available. |
| D — skip/quit | PARTIAL: lifecycle/fault tests pass; packaged interactive target scenario not run. |
| E — long telemetry | PASS for bounded diagnostic logger regression: capacity remains 1024 and `telemetry_truncated`, `sender_clean_known`, and `sender_clean` fail closed; no packaged long-session run. |
| F — partial/zero-progress fault injection | PASS for diagnostic/mock fault injection: Rust/Python fault tests pass with bounded retry and cleanup assertions; no packaged target run. |

Rollback selection evidence:

- Without the switch, `probe_native_dispatch()` reported the exact expected and actual build ID, source fingerprint, ABI, free-threaded status, and native availability.
- With `SKY_USE_PYTHON_DISPATCH=1`, `python_dispatch_explicitly_requested()` returned `True` while the native module remained available.
- `tests/test_native_dispatch_selection.py` and the native lifecycle tests passed.
- Full application rollback playback and post-rollback re-enable were not run as a packaged target-window scenario.

## Upgrade and failure evidence

- Public v2.4.5 release metadata was confirmed to exist with the canonical triple.
- The candidate v3.0.0 assets are not public because the release has not been tagged or published. Therefore an isolated real updater upgrade from a packaged v2.4.5 install to the v3.0.0 candidate was **NOT RUN**.
- Pester 61/61 covers manifest mismatch, corrupt/missing files, locked executable, rollback, transaction journal, and preserve-list behavior in isolated fixtures.
- Public candidate zip SHA256, public MANIFEST verification, transaction committed state, runtime upgrade report, and post-publish updater channel checks remain pending.

## Evidence boundary

This release validates sender-side dispatch, lifecycle, packaging and updater
contracts. It does not claim direct game-observed or audio-onset timing evidence.

## Release gate status

Local code/static/native/frozen gates above passed on the code candidate SHA.
GitHub Actions has not run on the final merge SHA, the Windows packaged soak is
not complete, and the v2.4.5-to-v3.0.0 public-asset upgrade has not run.

Current recommendation: **NO-GO**. Do not create or push `v3.0.0` until the
missing exact-SHA CI, packaged lifecycle soak/focus evidence, and candidate
asset upgrade/rollback evidence are completed.
