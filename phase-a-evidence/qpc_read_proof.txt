# QPC/read ordering proof

Deterministic sender test:

- `target_crossing_sample_is_reused_without_a_second_pre_call_read`
- scripted QPC sequence: 9, 9, 10 (target crossing), then completion 11
- the send seam receives exactly start tick 10 and is called once
- no QPC sample remains after the crossing before the send seam

Static source test:

- `target_sender_has_no_qpc_read_between_crossing_and_sendinput`
- verifies payload pointer resolution occurs before the crossing
- verifies there is no `clock.now()` between the crossing and `SendInput(`

Cutoff tests also cover strict `>` rejection, equality acceptance, and the
Up-only late-release exception.
