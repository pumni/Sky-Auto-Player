[build-system]
requires = ["maturin>=1.13.3,<2"]
build-backend = "maturin"

[project]
name = "sky-player-rs"
version = "0.1.0"
requires-python = ">=3.14,<3.15"

[tool.maturin]
manifest-path = "crates/sky_player_rs/Cargo.toml"
bindings = "pyo3"
module-name = "sky_player_rs"
strip = true
