[toolchain]
# Stable production pin. rust-version in Cargo.toml remains the workspace MSRV.
channel = "1.98.0"
profile = "minimal"
targets = ["x86_64-pc-windows-msvc"]
components = ["rustfmt", "clippy", "rust-analyzer"]
