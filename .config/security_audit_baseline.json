{
  "$schema": "SECURITY.md mandate baseline — one entry per historical (path, line, rule) exception while the underlying code is migrated.",
  "comment": "Each entry must have a concrete justification and tracking reference. Remove an entry after the offending code is gone or refactored to a permitted API.",
  "exceptions": []
}
