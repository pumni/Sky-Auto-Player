# Sky sheet links

This directory contains curated external resources for finding or downloading
Sky-compatible music sheets.

The `.url` files are Windows Internet Shortcut files. These links are
convenience resources only; they are not application runtime or build
dependencies, and their external destinations may change or disappear
independently of this repository.
